name: Java CI with Gradle

on:
  pull_request:
    branches:
      - "main"
      - "develop"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write       
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          generate-job-summary: true

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build --no-daemon

      - name: Publish Test Results
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: "build/test-results/**/*.xml"
          comment_title: "🚀 CI 테스트 결과 리포트" # 사용자화된 리포트 제목
          comment_mode: "update last"         # 이전 코멘트를 업데이트하여 PR 타임라인 관리
          test_changes_limit: 10              # 상세 변경 내역 행 제한 (접기 기능 활용)
          report_individual_runs: "true"      # 개별 실행 결과 포함

spring:
  application:
    name: PlayProbie
  profiles:
    active: local # 기본적으로 local 프로필을 활성화

# Actuator 설정 (Health Check용)
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    groups-order: DESC
    operations-sorter: method
    disable-swagger-default-url: true
    display-request-duration: true

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:}

playprobie:
  base-url: ${PLAYPROBIE_BASE_URL:http://localhost:5173}

# JWT 설정
jwt:
  secret: ${JWT_SECRET:ZGV2LXNlY3JldC1rZXktZm9yLWxvY2FsLWRldmVsb3BtZW50LW9ubHktMzItYnl0ZXM=}
  access-token-expiration: 3600000  # 1시간 (ms)
